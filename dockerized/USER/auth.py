# auth.py — Flask Blueprint (removed)
# Authentication routes have been converted to FastAPI and moved to:
#   routers/auth.py  — /login /signup /logout + /auth/token /auth/refresh /auth/me /auth/register
# DB operation routes (restore/status/attachdb) are in:
#   routers/main_router.py
